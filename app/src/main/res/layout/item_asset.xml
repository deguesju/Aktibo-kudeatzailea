<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/bg_glass_card_gradient"
    android:padding="16dp"
    android:layout_marginBottom="12dp"
    android:elevation="6dp">

    <!-- Icono del activo -->
    <FrameLayout
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@drawable/bg_glass_bubble"
        android:layout_marginEnd="16dp">

        <ImageView
            android:id="@+id/image_icon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="Icono Activo"
            app:tint="#FFD700" />
    </FrameLayout>

    <!-- Info del activo -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_weight="1"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/text_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Bitcoin"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium"/>

        <TextView
            android:id="@+id/text_symbol"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="BTC • Cripto"
            android:textColor="#DDDDDD"
            android:textSize="13sp"
            android:fontFamily="sans-serif-light"/>
    </LinearLayout>

    <!-- Valor y porcentaje -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_vertical|end"
        android:layout_marginEnd="8dp">

        <TextView
            android:id="@+id/text_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1.240€"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:fontFamily="sans-serif-medium"
            android:gravity="end"/>

        <TextView
            android:id="@+id/text_percent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="+2.5%"
            android:textColor="#4CFF7E"
            android:textSize="14sp"
            android:fontFamily="sans-serif-medium"
            android:gravity="end"/>
    </LinearLayout>

    <!-- Botón agregar -->
    <ImageView
        android:id="@+id/button_add"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="12dp"
        android:padding="8dp"
        android:background="@drawable/bg_glass_bubble"
        android:src="@drawable/ic_add"
        android:contentDescription="Agregar a distribución"
        app:tint="#FFFFFF"/>
</LinearLayout>
